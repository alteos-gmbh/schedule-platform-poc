{
  "name": "poc-schedule-console",
  "private": true,
  "type": "module",
  "description": "The local console for the schedule platform PoC: a static page plus a proxy that turns a browser fetch into one lambda:Invoke.",
  "scripts": {
    "console": "node proxy.mjs",
    "test": "node src/test.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.700.0"
  }
}
